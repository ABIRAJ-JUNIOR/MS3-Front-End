import { AssesmentStatusSortPipe } from './assesment-status-sort.pipe';

describe('AssesmentStatusSortPipe', () => {
  it('create an instance', () => {
    const pipe = new AssesmentStatusSortPipe();
    expect(pipe).toBeTruthy();
  });
});

import { SearchContactUsPipe } from './search-contact-us.pipe';

describe('SearchContactUsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchContactUsPipe();
    expect(pipe).toBeTruthy();
  });
});

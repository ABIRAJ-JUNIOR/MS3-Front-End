import { SearchMycoursePipe } from './search-mycourse.pipe';

describe('SearchMycoursePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMycoursePipe();
    expect(pipe).toBeTruthy();
  });
});
